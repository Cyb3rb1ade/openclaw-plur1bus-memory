# Hermes 7.12.47 Upstream Integration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development.

**Goal:** Integrate exact upstream 7.12.47 without losing reviewed Hermes code.
**Architecture:** Add a normal merge and supplemental delta inventory; native
maintenance implementation and final package gates follow separately.
**Tech Stack:** Git, Node, Python, existing version/inventory checks.
**Spec:** `docs/superpowers/specs/2026-09-11-hermes-7.12.47-addendum.md`

## Global Constraints

- Upstream `8a148c991123be31bc4f99376c8198447d9bb717`; historical 7.12.44
  audit pin and evidence remain unchanged.
- JS/dashboard `7.12.47-hermes.0`, Python `7.12.47`; unpublished candidate.
- No production profile/model/data writes, release, tags, push, registry change,
  amend, reset or force operations. Preserve unrelated work and host additions.
- All new behavior needs regression evidence; a JS merge is not native parity.

## Task 1: Merge source, align candidate versions, record delta

**Files:** upstream's 14 changed paths, existing active candidate version fields
in Python pyprojects/plugin manifests and dashboard package manifests; create
`docs/audits/hermes-7.12.47-delta-review.md` and
`plur1bus-hermes/tests/test_upstream_747_inventory.py`.

**Interfaces:** Retain historical inventory and validator; new supplemental
inventory proves target ancestry and version consistency without promoting
unimplemented native contracts.

- [ ] Record clean HEAD and diff; merge exact pin using `git merge --no-ff --no-commit
  8a148c991123be31bc4f99376c8198447d9bb717`. Resolve version conflicts to
  candidate identifiers; preserve Hermes-specific manifest fields. Unexpected
  runtime conflict requires controller context, not taking theirs globally.
- [ ] Add a regression checking current package/pyproject/plugin/dashboard
  versions against the new target and exact ancestry:

```python
assert subprocess.run(["git", "merge-base", "--is-ancestor",
    "8a148c991123be31bc4f99376c8198447d9bb717", "HEAD"]).returncode == 0
```

  Before merge commit, ancestry assertion must fail; record that RED separately
  from version assertions. After commit it must pass. Do not break historical
  audit assertions by replacing their pinned source with current version.
- [ ] Align only active version metadata, including lockfile root versions.
  Do not rewrite historical docs or audit evidence via global substitution.
- [ ] List all 14 changed paths and three release contracts with upstream
  functions, native candidate paths and honest pending status in the new audit.
  Record risky two-statement decay/fallback behavior as requiring native-safe
  implementation, not as verified parity.
- [ ] Commit merge and focused metadata/tests; run upstream changed-file test
  suites (`tests/decay-batch.test.js`, `tests/db-adapter-timeouts.test.js`,
  `tests/daily-consolidation-decay-cursor.test.js`, `tests/lancedb-optimize.test.js`,
  `tests/release-750-compat.test.js`), inventory test and `git diff --check`.
  Test actual behavior, not only presence of functions. Preserve failure output.
- [ ] Self-review, report exact commits/commands/output and remaining native work.
