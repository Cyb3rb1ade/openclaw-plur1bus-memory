# Hermes 7.12.44 Reminder Contracts Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add deterministic relative reminder proposals, prevent recursive nudge extraction and make confirmation replay-safe.

**Architecture:** Parse precise relative durations from the real user turn using its durable capturedAt timestamp. All reminders remain private pending proposals until existing authorized confirmation. Keep absolute-date support; never infer vague dates or authority from assistant text.

**Tech Stack:** Python, scoped JSONL, native metadata and controls, deterministic clocks.

**Spec:** `docs/superpowers/specs/2026-09-11-hermes-7.12.44-design.md`

## Global Constraints

- Pin upstream `a3f48f28ac647e81c5260e8a1dbab7977bf9fb51`, Hermes baseline `c12ec2bba63d74ac8add8782ab6761472b4149c6`.
- No production activation, live migration, model downloads, publication or registry changes.
- Preserve reminders.autoExtract explicit opt-in, private-scope-only policy, exact card/scope authorization and explicit scheduling confirmation.
- Required macOS ARM64, Windows ARM64, Windows x64, Linux x64; retain Linux ARM64 tests, no Intel macOS.
- After capture identity task; TDD per changed behavior; temporary fixture homes only.

## Task 1: Deterministic relative proposals and extraction-loop guards

**Files:** create `plur1bus-hermes/src/plur1bus_hermes/reminder_extraction.py`; modify `domain.py` extract_reminder_proposals/on_turn; extend `test_cognitive_opt_in_and_reminders.py`; create `test_reminder_extraction.py`.

**Interfaces:** Extend extract_reminder_proposals with optional `capture_id` and `captured_at`. Add pure `parse_relative_reminder(user_text, captured_at) -> dict | None` returning dueAt/evidence/topic. Existing absolute parser remains separate. Candidate ID uses durable captureId plus parsed occurrence, not fresh random per retry.

- [ ] Add precise fixed-clock failing regression:

```python
parsed = parse_relative_reminder("Erinnere mich in 10 Minuten an den Tee", "2026-09-11T12:00:00Z")
self.assertEqual(parsed["dueAt"], 1789128600000)
self.assertIn("Tee", parsed["topic"])
self.assertIsNone(parse_relative_reminder("bald", "2026-09-11T12:00:00Z"))
```

- [ ] Run RED, implement DE/EN positive finite minutes/hours/days plus half-hour. Literal fixture independently verified with Node Date.parse("2026-09-11T12:10:00Z") =1789128600000. Require genuine explicit reminder request and substantive topic outside time phrase; reject bare duration, vague bald/später, negation and assistant-only promise. Preserve exact matched span, not reconstructed phrase that fails topic removal.
- [ ] Strip complete `<reminder-nudge>` blocks from user evidence; malformed/unclosed markers fail closed for extraction instead of exposing tail text. Assistant/system/tool content cannot create proposals. Absolute LLM extraction receives user evidence only, must return source substring evidence grounded in that user request, and passes same nudge/topic checks.
- [ ] Use capturedAt for elapsed duration so retry never pushes due time forward. If timestamp absent direct API uses one captured time per call, recorded in proposal; queued runtime always supplies durable capture identity. Past/overflow/>5-year results rejected. Precise elapsed days remain24h, not guessed local-calendar appointment.
- [ ] Persist at most3 pending proposals with source capture/turn fingerprint and deterministic ID. No backend call required for supported relative phrases; absolute route retains existing backend opt-in and bounded text. Dedupe is per admitted source, not all identical text forever.
- [ ] Test DE/EN singular/plural/half-hour, exact topic sentence, unicode, zero/negative/overflow, no-topic, nudge loop and malformed nudge, assistant-only, retry after clock advance/restart, foreign/shared rejection, disabled/no-backend relative path and existing absolute cases. Commit with documented stricter confirmation/request policy versus upstream auto-scheduling.

## Task 2: Latest-state confirmation and due-card lifecycle

**Files:** modify domain confirm_reminder_proposal/create_reminder/due_reminders; extend existing reminder/controls tests and new `test_reminder_replay.py`.

- [ ] Reproduce pending-event resurrection: confirm a proposal, then repeat. Assert no second metadata schedule or confirmation event:

```python
first = domain.confirm_reminder_proposal(proposal_id, memory_id)
second = domain.confirm_reminder_proposal(proposal_id, memory_id)
self.assertTrue(first["created"])
self.assertFalse(second["created"])
self.assertEqual(second["reason"], "proposal-already-confirmed")
```

- [ ] Run RED. Select latest scoped proposal state by ID before checking pending status; confirmed/cancelled/stale transitions are terminal for old request. Preserve one-use controls nonce; durable ledger protection is additional, not a substitute for authorization.
- [ ] Revalidate exact authoritative card active/TTL/scope and proposal due time before metadata mutation. Pending/confirmed reminder metadata on a deleted/expired/foreign card never becomes due output. Never treat metadata read failure as an empty successful authoritative read.
- [ ] Keep memory-writer lock and per-row update. Record prepared schedule identity so crash after metadata commit but before confirmed event is recoverable without redispatch; verify same reminderKey/card/dueAt at restart, otherwise fail closed. Do not rewrite whole metadata table or repeat live delivery.
- [ ] Test repeated confirmation, new confirmation after cancelled/latest event, expired/deleted source, changed proposal/card, failure between metadata and ledger, restart reconciliation, foreign identity and normal due lifecycle. Run controls/proactive tests to preserve authorized route and governor behavior. Commit.

## Host boundary

No durable cold-start route is invented. Native proactive delivery still requires
a verified currently authorized Hermes route event. Relative parsing and scheduling
correctness do not prove guest-runtime delivery or generic host reaction support.
