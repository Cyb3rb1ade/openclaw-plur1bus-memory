# Hermes 7.12.44 Candidate Verification Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reconcile the full feature surface, prove Hermes-specific preservation and build unpublished candidate artifacts with truthful platform evidence.

**Architecture:** Keep immutable upstream/baseline inventory separate from current native contract evidence. Verify source, installed fixture, package and guest runtime as distinct surfaces. No public release or production cutover is implied.

**Tech Stack:** Python/Node test suites, native distribution builder, temporary Hermes fixtures, existing VM test surfaces.

**Spec:** `docs/superpowers/specs/2026-09-11-hermes-7.12.44-design.md`

## Global Constraints

- Pin upstream `a3f48f28ac647e81c5260e8a1dbab7977bf9fb51`, Hermes baseline `c12ec2bba63d74ac8add8782ab6761472b4149c6`.
- Candidate JS/dashboard7.12.44-hermes.0, Python7.12.44. Explicitly unreleased; no tags, registry changes, production profile activation or live memory/model migration.
- Required macOS ARM64, Windows ARM64, Windows x64 and Ubuntu/Linux x64; preserve Linux ARM64 coverage, no Intel macOS edition.
- All preceding runtime/history/workflow plans reviewed before final gate. Remaining host limitations require precise evidence, not optimistic exclusions.
- No warning/audit gate suppression or inherited signing/platform claims.

## Task 1: Complete contract-by-contract current audit

**Files:** audit JSON/Markdown, `plur1bus-hermes/src/plur1bus_hermes/parity.py`, inventory/parity tests; create `docs/audits/hermes-7.12.44-surface-map.json` and test `test_upstream_surface_map.py`.

- [ ] Enumerate pinned plugin manifest config-schema property paths recursively, declared tools/CLI/provider contracts, and actual command/hook/job registrations from pinned source. Snapshot command aliases and internal subcommands too, not just top-level names. Exclude private credentials/values; this is public schema only.
- [ ] Add failing independent set-equality tests for every manifest config path and declared contract:

```python
self.assertEqual(set(surface_map["configPaths"]), set(pinned_schema_paths))
self.assertEqual(set(surface_map["tools"]), set(pinned_manifest["contracts"]["tools"]))
```

- [ ] Map each surface to native entry point/config conversion or exact host limitation. Every legacyFEATURES/COVERAGE_710/COVERAGE_712 row and all37 upstream commit rows need reviewed current status, nonblank source/native evidence and relevant tests. A single commit spanning several contracts stays partial until all applicable parts verified.
- [ ] Explicitly inspect workspace policy, knowledge_update, configured provider families, semantic/temporal merges, repair/cutoff workflows, review jobs and language/timezone settings not covered by narrower plans. If a concrete native-achievable gap remains, produce and execute a bounded TDD task before finishing this audit; do not label it host-specific for convenience.
- [ ] Reconcile every open item in `docs/audits/hermes-7.12.44-native-gap-ledger.md`.
  In particular, same-dimension runtime embedding fallback and central-route
  identity bypass require an owning implementation fix, not only a package
  test. Execute provider identity/reranker validation/capability/profile tasks
  before certifying retrieval or staged migration preservation.
- [ ] Update parity.coverageVersion to7.12.44 and reflect actual statuses. Separate native operational readiness from exact upstream equivalence and unverified guest/model quality. Preserve immutable historical fields, no rewriting old releases as newly verified.
- [ ] Run new inventory/parity tests and review. Current matrix may honestly retain documented host limits and intentional safety differences; it may not hide an unimplemented required native contract.

## Task 2: Hermes preservation and source regression gates

**Files:** existing provider/controls/dashboard/distribution tests and fixture helpers only as required by concrete regressions; current audit report.

- [ ] Run full Python/controls/dashboard/distribution suites with read-only existing interpreter or disposable QA venv. Record full command, dependency environment and warnings separately. Run full Node suite, lint, dependency audit and diff check against exact candidate commit.
- [ ] Exercise fixture profile switching with default/all/selected setup: activated profile shows button only when provider and both plugins active; inactive profile stays inactive; stale requests after switch cannot mutate prior profile. Test source and rendered dashboard separately where native UI available.
- [ ] Exercise BGE/Jina nano provider workflows, same-dimension different-encoder rejection, all selected-agent staged reembedding, metadata/ACL/validity/tombstone/epistemic retention, snapshot/drift/lease/recover/rollback and explicit download/license gates. No production home used.
- [ ] Inspect all preserved baseline distribution/host-loader behavior with current code checksums, not size/mtime. Confirm embedded macOS Python -I -B, explicit all/default/selected activation, safe rollback receipts, no generated bytecode.
- [ ] For every failing new regression, RED/GREEN scoped fix and independent review; inherited warnings/security issues remain explicitly separate. Current npm audit roots adm-zip/sharp must be freshly rechecked and resolved or reported as candidate blockers, never silently waived.

## Task 3: Candidate build and installed-artifact tests

**Files:** distribution/build.py/verify.py manifests and current acceptance docs only if changes needed; candidate outputs outside source using mktemp.

- [ ] Read builder/verify CLI help and current distribution README; build both Python wheels and portable candidate bundles using existing scripts. Run npm pack inspection. Record exact artifact names, source commit/tree, SHA256 and package member list.
- [ ] Verify all new native modules/manifests included, dependency constraints coherent, no secrets/data/backups/pycache/build residue in source package. Wheel install into disposable home uses forced checked file refresh when appropriate; compare installed module hashes with artifact.
- [ ] Run installed-artifact MemoryProvider lifecycle capture/flush/recall, short Neo candidate, migration fixture, profile controls/dashboard and rollback smoke. Import-only success is not runtime success; stub-model storage tests are not real-model benchmarks.
- [ ] Build macOS ARM candidate PKG through existing assistant flow; inspect expanded payload and receipt. Do not inherit old signing/notarization state. New signing/notarization/publication remains separate later gate under current approved spec.

## Task 4: Platform evidence and final review

**Files:** current audit/platform acceptance report and candidate manifest.

- [ ] Inspect available test VM/container surfaces before use. Test Windows x64 on Windows x64, Windows ARM on Windows ARM, Ubuntu/Linux x64 on x64 and macOS ARM locally; retain Linux ARM coverage where available. Do not infer arch from a launcher name or reuse previous release evidence.
- [ ] Capture guest architecture/interpreter/package hashes and actual command output for install/import/storage/lifecycle/provider/job tests. Graphical installer/profile/settings/migration preview additionally needs visible UI checks. If guest unavailable, name exact unverified gate and exhaust safe alternative diagnostics; no fabricated pass.
- [ ] No real-model download without scoped test destination and applicable license consent. Preserve existing user models/profiles. Bound acceptance claims to tested encoder/reranker and exact artifacts.
- [ ] Request strongest-model independent whole-candidate review, then root final review of actual wiring, negative cases, package contents, audit completeness and residual boundaries. Fix relevant findings through original worker/new bounded task with immutable commit snapshots.
- [ ] Deliver unpublished candidate artifacts, exact source pins, feature map, measured platform/test results and precise remaining host/security/signing limitations. No GitHub release, tag, registry mutation or production install without the separate later publication/cutover instruction.
