# Hermes 7.12.44 Historical Recall Contracts Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close verified correction-lineage and recall-provenance gaps while reconciling cache and selector contracts without unsafe compatibility shortcuts.

**Architecture:** Reuse canonical scoped storage, existing additive recall workers, bounded prompt assembly and native caches. Preserve archived sources and explicit ownership; no second authoritative store, no forced configuration reset.

**Tech Stack:** Python, LanceDB, scoped JSONL, SQLite, unittest/pytest.

**Spec:** `docs/superpowers/specs/2026-09-11-hermes-7.12.44-design.md`

## Global Constraints

- Upstream pin `a3f48f28ac647e81c5260e8a1dbab7977bf9fb51`; Hermes baseline `c12ec2bba63d74ac8add8782ab6761472b4149c6`.
- No production installation, live migration, model downloads, publication or registry changes.
- All mutation routes require existing scope/ACL/revision guards; archive-first deletion and tombstone behavior remain.
- Required macOS ARM64, Windows ARM64, Windows x64, Linux x64; retain Linux ARM64 coverage; Intel macOS excluded.
- TDD for changed behavior; existing read-only Python with -B and temporary fixtures. Run after storage/episodes/cognition units.

## Task 1: Durable native correction lineage

**Files:** modify `runtime.py` correction transaction; create `plur1bus-hermes/src/plur1bus_hermes/correction_lineage.py`; extend `domain.py` metadata/graph materialization; tests `test_correction_reinforcement.py`, new `test_correction_lineage.py`.

**Interfaces:** Preserve correct_async boolean public API. Add scoped durable relation containing correctionId, sourceId, replacementId, sourceFingerprint, sourceArchiveDigest, scopeKey, agentId, prepared/committed state. Expose relation through replacement metadata `correctionOf` and read-only recall explanation, not by altering source text/authority. Reuse original replacement ID on retry of a prepared transaction; changed source cannot create an unbounded fork.

- [ ] Add regression correcting a scoped card with validity, expiry, custom importance and user metadata. Require an inspectable replacement-to-archive relation and preserved nonderived metadata:

```python
self.assertTrue(runtime.correct_async(source_id, "corrected fact", "session"))
replacement = active_replacement()
self.assertEqual(metadata_for(replacement["id"])["correctionOf"], source_id)
self.assertEqual(replacement["validFrom"], original["validFrom"])
self.assertTrue(verified_source_archive(source_id))
```

- [ ] Run RED. Under existing cooperating-writer lock prepare durable source/replacement identity and source snapshot before retirement. Preserve current rule: replacement must be durably prepared before source is forgotten; deletion itself stays archive-first. Never delete original on a failed lineage/metadata/graph write.
- [ ] Copy nonderived source metadata explicitly: importance/manual-core flags, half-life policy, category/type and human annotations. Keep source timestamps as source evidence, not replacement capture time or validity guesses. Recompute content-derived classification/embedding and clear stale automatic reminder execution state; preserve original reminder evidence without silently rescheduling it. Record per-field carry/recompute rules in audit.
- [ ] Preserve ACL/epistemic/validity/expiry semantics without manufacturing observed trust. Both relation sides must match exact scope. Verify replacement and lineage before retiring source, then mark committed; on restart reconcile prepared relation only against exact source/after state. Unexpected forks/drift fail closed. Existing orphan-replacement archive path remains in force.
- [ ] Preserve graph evidence: retain historical source edges; add explicit version relation to replacement; do not copy semantic edge scores to changed text or reactivate tombstoned source via graph hydration. Scoped explanations must not print archived source content by default.
- [ ] Test restart after each durable boundary, failed metadata/lineage write, concurrent source edit/forget, repeated same command, foreign same-ID source, manual/core metadata, expiry/validity, unchanged tombstones, and actual recall/graph explanation. Commit scoped files and exact evidence.

## Task 2: Paired contradiction gates on every prompt path

**Files:** modify `domain.py` recall_overlay/contradiction_context as necessary; tests `test_recall_overlay.py`, `test_live_contradiction.py`, `test_contradiction_disclosure.py`.

- [ ] Add negative regression with a scoped pair where only one member passed recall:

```python
overlay = domain.recall_overlay([visible_card], acl_bindings=binding)
self.assertNotIn(hidden_partner_id, overlay)
```

- [ ] Observe RED; require both exact IDs among current scoped lifecycle-valid rows in overlay, matching contradiction_context. Do not query a missing partner to bypass current recall selection. No untrusted ledger text may create a disclosure ID.
- [ ] Preserve escaping and400-character disclosure cap. If emitting multiple pairs, select deterministic latest pairs and stop within the same total cap, never replace primary recall. Test one-sided/foreign/expired pair exclusion, stale resolved latest event and permitted pair output. Commit only reachable fix and tests.

## Task 3: CRR provenance through bounded prompt assembly

**Files:** modify `plur1bus-hermes/src/plur1bus_hermes/domain.py` booster return metadata and `plur1bus-hermes/src/plur1bus_hermes/runtime.py` prompt assembly; existing selector helpers remain in domain.py; tests `test_runtime_recall_additive_scopes.py`, `test_recall_feature_gates.py`, `test_recall_deadline.py`; create `test_reactivation_projection.py`.

**Interfaces:** Keep boost_recall list return compatibility; append safe internal `_plur1busRecallOrigin` marker on copied extra rows only (graph/lens/reactivation), never authoritative table rows. Runtime treats CRR-origin copies as historical untrusted evidence in a separate `<memory-reactivation>` block. Source marker is internal provenance, never an ACL grant. Duplicates of primary results retain primary status.

- [ ] Add reachable recall regression with primary plus CRR-only result:

```python
text = runtime.recall("continue this", session_id="session")
self.assertIn("<memory-reactivation>", text)
self.assertIn("Historical evidence", text)
self.assertIn(primary_text, text)
```

- [ ] Run RED. Carry provenance through booster hydration/dedup/rerank without modifying main rows. Format escaped CRR content with explicit noninstruction semantics; optional visibleHints stays defaultfalse. Count block content inside existing globalInjectMaxChars, prioritize primary rows, discard optional block if budget insufficient.
- [ ] Preserve RAM-only scoped/session trigger and cooldown state,50ms caller budget and bounded worker/no backlog, no memory writes. Add faded/community/open-thread selection only from current scoped existing indexes/journal projections, with caps3/1/3/2; missing sources leave base recall unchanged, never scan foreign data. Each selected memory must rehydrate through existing lifecycle gate.
- [ ] Test successful provenance, duplicateprimary, tiny budget, timeout/failureunchangedprimary, foreign and stale IDs, repeated trigger/cooldown, hints opt-in and actual provider session routing. Record sources not natively represented as remaining partial rather than invented entries. Commit.

## Task 4: Lens relevance and cache contract reconciliation

**Files:** `domain.py` existing lens selector, `cache.py`, `llm_cache.py`, `llm_backend.py`, `operator_status.py`, existing related tests; new `test_cache_shutdown.py` only if needed.

- [ ] Write lens regression with two scoped indexed communities: query-token-related candidate versus unrelated candidate. Require unrelated entry absent while relevant entry remains additive. Implement upstream-style bounded overlap selection before hydration; retain native default-off and existing caps/deadline.
- [ ] Add cache metrics opt-in tests using existing enabled/disabled config and status route:

```python
first = embedder.embed("scoped text")
second = embedder.embed("scoped text")
self.assertEqual(first, second)
self.assertEqual(status()["cacheMetrics"]["embedding"]["hits"], 1)
```

- [ ] Reconcile `embeddingCacheMetrics`/`llmResultCacheMetrics` with native config precedence and status redaction. Report only current-scope counts; never prompt/key/hash/credential data. Include persisted hit/write/skip/coalesced counters and LLM avoided usage components when provider supplies them; missing usage is unknown, not fabricated zero savings.
- [ ] Preserve existing safe native defaults and routing/purpose/model fingerprint key partitions. Document128/5min upstream versus4096/7d native and other actual inspected limits; do not invalidate user choices just to match a numeric default. Do not lowercase case-sensitive encoder inputs for superficial key equivalence.
- [ ] Add controlled blocked-compute shutdown test. Verify no use-after-close, followers receive result/error, close has documented bounded/drain behavior and deadlock-free lock ordering. If current code already satisfies actual runtime shutdown contract, retain it with evidence; if it fails, fix lifecycle under focused RED/GREEN. Audit extra native LLM purposes individually: deterministic only, unknown bypass, no chat/dream narrative/critical classification caching.
- [ ] Run scoped booster/cache/backend/operator tests, record intentional mapping differences and pending model quality gates, commit. Index read-cache optimization is optional only if measured/read-bound evidence warrants it; do not grow this into a new cache framework.

## Review and audit

Update current audit fields only after each reviewed task. Historical source groups remain immutable. `parity.py` must describe actual broader caller behavior, not only the best-tested helper. Default differences and host interface gaps are explicit native decisions, never silently promoted to full equivalence.
