# Hermes 7.12.47 Installed Package Verification Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development.

**Goal:** Build inspectable candidate packages and verify installed adapter
lifecycle, not just imports or direct private storage calls.
**Architecture:** Extend existing disposable install/rollback gate and ship a
truthful candidate status document. Keep source, artifact, installed fixture,
real models, guest architecture and public release as separate evidence.
**Tech Stack:** Python, existing distribution builder, platform QA interpreters.
**Spec:** `docs/superpowers/specs/2026-09-11-hermes-7.12.47-addendum.md`

## Global Constraints

- Candidate JS/dashboard7.12.47-hermes.0, Python7.12.47; upstream8a148c991123be31bc4f99376c8198447d9bb717.
- No production activation/data/model changes or publication. Use disposable
  homes and QAvenvs. Real model downloads are not part of this fixture.
- Do not hide earlier unfinished native contracts or npm audit findings.
- Required macARM,WinARM,Winx64,Linuxx64; retainLinuxARM; noIntelMac.
- Missing guests or signing gates remain unverified, never inferred from names.

## Task 1: Installed lifecycle and honest candidate metadata

**Files:** `distribution/verify.py`, `distribution/build.py`, distribution
tests, `distribution/README.md`, `distribution/INSTALLATION.de.md`; create
`distribution/CANDIDATE-STATUS.md` and optional focused
`distribution/lifecycle_smoke.py` if keeping the existing inline script would
be unwieldy. Every new helper used by artifacts must be copied by builder.

**Interfaces:** Preserve verify(bundle, executable=None) and current installer
confirmation/rollback semantics. Install selected fixture profile qa with root
default untouched; use installed Plur1busMemoryProvider, not source PYTHONPATH.

- [ ] Add RED test showing installed lifecycle smoke goes through
  Plur1busMemoryProvider.sync_turn then runtime.flush(10), rather than directly
  calling _remember with domain disabled. Assert configured profile isolation:

```python
statuses = {row["name"]: row for row in inspect_profiles(home, python)["profiles"]}
assert statuses["qa"]["active"] is True
assert statuses["default"]["active"] is False
assert (home / "config.yaml").read_bytes() == original_default
```

  Use actual existing inspect_profiles result shape, asserting qa active with
  bothplugins and default inactive. No truthiness-only test is sufficient.
- [ ] In isolated child after real wheel install, import installed provider
  and assert its file is within childvenv or installed fixture plugin paths.
  Configure absolute fixture dataDir, autoCapture/autoRecall true, embedding
  openai-compatible baseUrl http://127.0.0.1:9/v1 model package-qa-stub dimension2,
  disabled reranker and no llm.model. After provider.initialize(session,
  hermes_home=fixture_home,agent_identity="qa",agent_context="primary"), replace
  runtime._embedding.embed with deterministic [0.1,0.2] before any capture.
  Do not disable domain.on_turn/on_memory; this is the behavior under test.
- [ ] Call sync_turn with user 'My package QA marker is cobalt-orchid.' and
  assistant 'I will retain cobalt-orchid.', explicitmatching session/agent,
  then flush. Require2journalentries sharing onecaptureId, committed receipt
  containing2journaldescriptors+episode, realtable count>=2, runtime.recall and
  provider.prefetch both containcobalt-orchid. Assertions execute in child -I.
- [ ] Repeat identical publicsync_turn: expect4journalentries and2captureIds,
  each committed. This is2admissions, NOT retrydedup. Shutdown, create fresh
  provider over same fixture, stubembedding again and prefetch marker. Always
  finallyshutdown so no locks/threads leak into rollback.
- [ ] Keep retrieval-settings preview/apply smoke and selected-profile two-phase
  rollback. Restore qa configbytes, rootbytesunchanged, installedpluginfilesgone;
  piprollback remainsfalse. Do not claim realgateway/model/UIquality fromstub.
- [ ] Ship CANDIDATE-STATUS.md in portable/archive/nativeinstallerpayload with
  exactupstreampin/currentcandidateidentity, reviewedincrementalcontracts and
  explicit old7.12.44 nativegap-ledger reference, security/model/platform/signing
  boundaries. Current installationinstructions must distinguish candidate from
  old7.12.7release; preserve historical statements as historical.
- [ ] Add manifest upstreamCommit and candidateStatus="incomplete" fields,
  preserving manifest schema compatibility/checksums. No implied ready flag.
  Update builder tests for copiedstatusdoc/helper and requiredmodules; do not
  omit newmodules, include secrets/data/pycache, or use dirty source as finalbuild.
- [ ] Run focused distributiontests and realportablefixtureverification after
  building a disposable bundle; fix newregressions and report exactoutputs.
  Commit files, selfreview, returnreport. Subsequent controllerruns platform
  builds on frozenreviewedcommit and records hashes separately.

## Controller artifact gates after task review

Run configured npm test/lint/audit and all4Python suites at finalsource. Build
with existing distribution/build.py into newemptydirectories, inspect manifests,
wheel contents, SHA256SUMS and CLIentrypoints. macARM --mac-pkg; WinARM native
--windows-exe with both exacthashapprovedstoragewheels. Windowsx64 requires
Windowsx64 execution, Linuxx64 requiresLinuxx64; unavailablecurrentguests remain
explicitblockers, not replacedbyARMemulationclaims. Portableunqualifiedbundle
may be producedbutdoesnotcertify everyplatformdependency.

Existing signed-release capability is retained; no previous signature or
notarization is inherited. Candidateunsignednames/limits must be explicit.
Record artifact paths/sourcecommit/tree/testresults and independent finalreview.
Do not tag/publish/installproduction aspart of packagebuild.
