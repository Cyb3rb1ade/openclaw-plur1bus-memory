# Hermes 7.12.44 Graph, Obsidian and Workshop Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Preserve human-edited mirrors, add reviewed graph discovery/maintenance and make Workshop mining bounded and evidence-admissible.

**Architecture:** Extend existing scoped review manifests and one-use actions. Graphs remain derived evidence; canonical cards and human files are never silently overwritten. Workshop keeps Hermes approval/publish lifecycle and profile-wide visibility warning.

**Tech Stack:** Python, JSONL/manifests, LanceDB, native controls/dashboard actions, temporary fixtures.

**Spec:** `docs/superpowers/specs/2026-09-11-hermes-7.12.44-design.md`

## Global Constraints

- Pin upstream `a3f48f28ac647e81c5260e8a1dbab7977bf9fb51`, Hermes baseline `c12ec2bba63d74ac8add8782ab6761472b4149c6`.
- No production activation, live migration, downloads, publication or registry changes.
- Preserve canonical workspace restriction, profile/scope authorization, explicit action approval, source revalidation and no manual-file overwrite.
- Required macOS ARM64, Windows ARM64, Windows x64, Linux x64; retain Linux ARM64 tests, no Intel macOS.
- After storage/episodes/cognition/history units; TDD and independent review per task.

## Task 1: Conflict-safe outbound memory mirrors

**Files:** create `plur1bus-hermes/src/plur1bus_hermes/mirror_state.py`; modify domain `_write_obsidian_note`/`_update_graph_links`; extend `test_obsidian_maintenance.py`, create `test_mirror_conflicts.py`.

**Interfaces:** Scoped manifest records last plugin-written full digest plus managed graph-block digest per validated memory UUID. Mirror write returns written/unchanged/conflict, never rewrites a user revision. Legacy existing mirror without receipt is preserved and proposed for adoption only after exact preview/approval.

- [ ] Add RED: materialize mirror, edit body or graph block manually, materialize again, assert exact edited bytes unchanged and conflict review exists.

```python
before = note.read_bytes()
domain.on_remember(record, table)
self.assertEqual(note.read_bytes(), before)
self.assertEqual(latest_review(memory_id)["status"], "conflict")
```

- [ ] Implement bounded safe regular-file reads with path/symlink/revision checks and unique atomic writes. Update only matching known plugin revision; graph-link-only update may preserve manually edited body if the managed block itself still matches its receipt. Multiple/malformed managed blocks are conflict, not guessed ownership.
- [ ] Persist receipt only after verified file write; interrupted receipt reconciliation needs exact before/after digest, never blind adoption. Conflict artifact stores bounded hashes/IDs/reasons, not duplicated private content. Preserve manual sections and existing technical tags; use validated type/category/scope/workspace technical tags, not semantic content tags.
- [ ] Test existing manual file without receipt, stale receipt, repeated materialization, graph-block versus body edit, symlink/replacement race, failed write/receipt, scope separation, idempotent technical tags. Commit.

## Task 2: Structured Obsidian change/deletion reviews

**Files:** extend `obsidian_sync.py` and existing domain sync-state helpers; tests `test_obsidian_sync.py`; controls/dashboard existing Obsidian action branches only.

**Interfaces:** Preserve plan_obsidian_sync/runtime-only root selection and apply_obsidian_sync approved_revision. Versioned manifest distinguishes new/changed/deleted/conflict and immutable source metadata. Add an explicit reviewed deletion action; default import remains nondeleting.

- [ ] Add RED: acknowledged source disappears or changes structured decision; watcher produces review entry but no canonical mutation.
- [ ] Parse bounded frontmatter with existing safe YAML parser: validate ID/type/status/importance and preserve source provenance; frontmatter scope is descriptive, never grants a different destination. Unsupported or malformed metadata remains reviewable but cannot change card lifecycle silently.
- [ ] Complete-success scan required before reporting missing file; truncated/error scan never generates deletion. Store conflict/deletion state durably with source hash and destination IDs. Deletion apply uses existing authorized archive/tombstone flow after fresh exact revision confirmation, never file absence alone.
- [ ] Preserve stable-ID chunk imports and per-file post-read verification; include lifecycle action and config/scope/writer/source revisions in manifest hash. A changed-during-import source keeps old imported evidence recoverable but is not acknowledged as current.
- [ ] Test stale/profile-switched/reused approval, cross-scope frontmatter, source deletion, failed/truncated scan, malformed/symlink source, previously modified canonical card conflict and exact reviewed archive. Run desktop/web/CLI Obsidian action tests, commit.

## Task 3: Derived graph deduplication, edge families and conservative pruning

**Files:** create `graph_maintenance.py`; modify domain graph construction/read projection and jobs; create `test_graph_maintenance.py`, extend relevant domain/scope tests.

**Interfaces:** Deterministic scoped edge ID over endpoint IDs, edge type and direction; repeated materialization projects one latest edge. Families semantic/temporal/entity/emotional/contradiction remain typed. Graph pruning accepts a complete scoped live-ID snapshot plus expected graph revision, returns dry-run counts or explicit apply result. No invented upstream graph watermark contract.

- [ ] Add RED: materialize same source twice and assert same effective edge set; add same-session temporal and shared-entity/emotion examples with unrelated controls.
- [ ] Implement bounded edge construction using actual source metadata: temporal only same known session, entity only real extracted shared entities, emotional requires emotion plus lexical/entity overlap, never global shared-emotion links. Missing metadata does not authorize guessed entity/session. Keep existing semantic threshold and ACL endpoint hydration.
- [ ] Use complete successful scoped live-row enumeration before pruning. Preserve foreign/unknown legacy edges and episode anchors; failed/truncated enumeration cannot advance state or rewrite. Weak-old-unreinforced thresholds follow inspected upstream graph maintenance config; explicitly record native defaults in tests/audit before applying.
- [ ] Commit derived graph replacement atomically with expected revision and recoverable archived previous generation; validate both endpoints under current gates for recall. Add bounded native job using existing per-user gate; no arbitrary graph edit executor.
- [ ] Test duplicate/restart, each edge family's positive and negative cases, foreign preservation, live-scan failure/truncation, active/reinforced survival, local missing/inactive pruning, source drift and rollback. Commit.

## Task 4: Confirmed semantic discovery

**Files:** create `semantic_discovery.py`; modify scoped domain delegation and existing controls/dashboard action registry; create `test_semantic_discovery.py` and action regression.

**Interfaces:** `plan_semantic_discovery(runtime)` returns bounded candidate links, config/source fingerprints and revision; `apply_semantic_discovery(runtime, approved_revision)` only invoked through existing identity/scope/revision/one-use action validation. Existing daily structural rebuild never starts semantic discovery implicitly.

- [ ] Add RED: plan creates no mirror/index writes; valid confirmation writes reviewed links once; changed config/source/scope or reused action fails.
- [ ] Compute bounded ACL/lifecycle-filtered vector links at threshold0.78 by default, using only matching current encoder space. Preserve explicit/type tiers and manual managed blocks via mirror-state helper. Plan contains no foreign IDs or arbitrary paths.
- [ ] Revalidate before each staged write; on failure retain prior index and conflict-protect mirrors. Expose exact plan/apply controls and dashboard review using existing UX/action pattern, no generic shell command. Test config drift, expired/deleted row, encoder mismatch, foreign source, nonce reuse and failed atomic write. Commit.

## Task 5: Admissible, resumable Workshop mining

**Files:** modify `skill_workshop.py`, jobs and existing native job config; tests `test_skill_workshop.py`, `test_jobs.py`.

**Interfaces:** Keep mine/show/approve/publish lifecycle. Mining returns scanned/eligible/created/complete/nextCursor and explicit rate/lock skip, rather than silently scanning first100 active cards. Persist scope-bound continuation and outcome under existing lock conventions.

- [ ] Add RED: untrusted Obsidian/tool/merge evidence cannot generate proposal; eligible rows beyond first100 are eventually visited exactly once across bounded pages.
- [ ] Push scope/active/TTL/lookback/category eligibility before cap, then enforce observed/verified legacy epistemic admission using existing restore-safe cutoff. Never convert blank post-cutoff or untrusted source to observed. Allowed evidence categories decision/fact/strategy/preference/workspace_rule/user_preference; absent mappings get explicit native decision, not blanket admission.
- [ ] Defaults maxPerRun5,minConfidence0.6,minEvidenceScore3,lookbackDays30. Use bounded page/cycle state keyed scope+config and deterministic position; failed page leaves cursor unchanged. Existing older eligible evidence requires explicit cutoff policy, not automatic discard because its capture timestamp predates recent lookback.
- [ ] Add cross-process no-overlap and weekly completed-run gate; partial scan can resume without waiting a week, repeated completed cycle cannot call model. Persist cursor after processed page, rate timestamp only after complete cycle. Unknown/failed state is explicit skip/error, not reset.
- [ ] Optional native weekly miner under explicit existing skill opt-in, no automatically published skill. Retain evidence revalidation at mine/approve/publish and current profile-global warning. Tests cover stale/foreign/low-confidence/insufficient evidence, >cap continuation, crash/restart, lock/rate skip without LLM, disabled scheduling and source mutation blocking publish. Commit.

## Review and matrix

Obsidian/graph/Workshop are native-achievable gaps, not blanket host exclusions.
OpenClaw Workshop RPC/events have a different host contract; Hermes's explicit
approval/publication is the chosen equivalent, retaining profile-wide visibility.
Only reviewed implemented subcontracts may advance current audit status.
