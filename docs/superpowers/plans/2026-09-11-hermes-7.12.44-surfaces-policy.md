# Hermes 7.12.44 Native Tools and Workspace Policy Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close safe public-tool and workspace-policy gaps, and expose native locale/review workflows through authenticated Hermes surfaces.

**Architecture:** Reuse MemoryProvider dynamic tool schemas, profile-bound dashboard actions and native command confirmations. Workspace policy keys come from verified resolved runtime context, not model parameters. Hermes keeps chat-model/routing/delivery ownership.

**Tech Stack:** Python, scoped atomic JSON/CAS, native controls/dashboard, ZoneInfo, temporary host fixtures.

**Spec:** `docs/superpowers/specs/2026-09-11-hermes-7.12.44-design.md`

## Global Constraints

- Pin upstream `a3f48f28ac647e81c5260e8a1dbab7977bf9fb51`, Hermes baseline `c12ec2bba63d74ac8add8782ab6761472b4149c6`.
- No production activation, live migration, model download, publication or registry changes.
- Never allow tool arguments to choose agent/scope/origin authority or bypass deletion confirmation.
- Required macOS ARM64, Windows ARM64, Windows x64, Linux x64; retain Linux ARM64 tests; no Intel macOS.
- Execute after native lifecycle/history/workflow plans. TDD and independent review per task.

## Task 1: Safe tool aliases and source provenance

**Files:** `provider.py`, runtime store path and domain metadata, provider/schema tests; new `test_tool_provenance.py`.

- [ ] Add RED: schema contains memory_search and dispatch matches memory_recall for same scoped query/limit/validAt/full_text. Host source verified dynamic schemas at agent/memory_manager.py get_tool_schemas/handle_tool_call; no invented host callback required.
- [ ] Reuse one schema/dispatch construction for both names; identical validation, limits, errors and ownership. No duplicate recall implementation.
- [ ] Add optional store category/sourceUrl/evidenceQuote fields with bounded validation. Category uses existing documented native enum/mapping; sourceUrl accepts only explicit http/https URL as provenance, never fetched; evidenceQuote is data, not a system directive. Preserve role=tool and untrusted epistemic; never accept model-selected scope/origin/agent.
- [ ] Thread metadata through queued capture without dropping it on retry; idempotent legacy schema defaults and full metadata retention in export/reembedding/correction. Verify actual stored fields and recalled provenance, not only schema parsing.
- [ ] Test alias equality, foreign-scope negative case, malformed metadata/URL/oversize, retry persistence, sourceRole/epistemic cannot be escalated, old callers and migration fixture. Preserve memory_forget/memory_correct denial through model tools; authorized controls remain their safe functional route. Do not add a misleading read-only knowledge_update alias: map native knowledge propose/confirm explicitly as an intentional approval-bound difference. Commit.

## Task 2: Workspace policy storage and lifecycle guards

**Files:** create `workspace_policy.py`; modify resolved provider/runtime guards; tests new `test_workspace_policy.py` and provider/runtime fixtures.

**Interfaces:** Atomic per-data-root policy keyed by validated agentId + canonical resolved workspaceIdentity. `read_policy(binding) -> {enabled, revision}` defaults enabled only when no record exists; corrupt state fails closed. `set_policy(binding, enabled, expected_revision)` is internal mutation called only by authenticated operators. Workspace identity comes from existing runtime namespace/host-config resolution; absent identity is explicit unavailable capability, never a caller path or global wildcard.

- [ ] Add RED: disabled resolved workspace suppresses automatic capture, prefetch/recall and explicit memory tools; another agent/workspace remains enabled.

```python
policy.set_policy(binding, False, expected_revision=0)
provider.sync_turn("private fact", "reply", session_id="session")
self.assertEqual(owned_memory_count(), 0)
self.assertEqual(provider.handle_tool_call("memory_recall", {"query": "fact"}), disabled_result)
```

- [ ] Implement versioned atomic JSON with existing safe path/symlink/CAS/lock patterns. Read/status never creates state. Do not clear or migrate memories when disabling. In-flight writes recheck policy before durable admission/commit; already committed data stays intact. Pending retry remains preserved while disabled, not submitted or marked success.
- [ ] Gate initialize exposure, sync_turn, prefetch, recall and tool dispatch through one shared policy decision. Recheck at actual runtime operation to reject stale provider snapshots after policy change. Policy never relaxes ACL, it only narrows eligibility.
- [ ] Test CAS race, corrupt/symlink state, cold restart, disable during queued capture/prefetch, stale config, foreign workspace and same-path/different-agent isolation. Missing identity cannot accept policy mutation; report exact reason. Commit.

## Task 3: Authenticated policy and memory/knowledge dashboard actions

**Files:** controls plugin, operator_cli.py, dashboard plugin_api/action handlers and existing frontend templates; extend controls/dashboard session action tests.

- [ ] Add RED: workspace status read-only works; enable/disable preview binds identity+profile+workspace+revision; confirm applies once and stale/reused requests fail.
- [ ] Add `/plur1bus workspace status|enable|disable` through existing mutation authorization/confirmation, plus local explicit CLI. Dashboard status/set uses current-profile backend resolver and one-use action registry; no OpenClaw RPC shim.
- [ ] Expose existing safe forget/correct and knowledge propose/confirm via explicit preview/review actions. Preserve archived source, snapshot revision and identity confirmation. No generic command executor or unrestricted backend URL/file path input.
- [ ] Test profile switch between preview/apply, foreign IDs, malformed/replayed nonce, concurrent source/policy change, disabled workspace and current active profile button visibility. Test rendered UI where available, not endpoint schema alone. Commit.

## Task 4: Locale and native review bundles

**Files:** create `review_bundle.py` and focused locale helper; modify configuration normalization, domain/jobs/controls/dashboard review routes and Python dependency manifests only for required cross-platform IANA timezone data; tests new `test_review_bundle.py` plus jobs.

**Interfaces:** Validate language de/en (defaultde) and IANA timezone (defaultEurope/Berlin). Render native UI/system labels in selected locale, never translate stored facts silently. `build_review_bundle(runtime, kind, now)` reads only current eligible scoped cards/tasks and yields bounded morning/evening review evidence; no implicit memory mutation or new chat-model owner.

- [ ] Add RED with fixed UTC clock spanning Europe/Berlin date/DST boundary and German/English labels. Invalid zone yields explicit configuration error before scheduling, not host-local silent fallback.
- [ ] Add manual morning/evening review commands and dashboard view using authenticated current scope. Defaults09:00/18:00 are schedule metadata, not evidence of exact native delivery. Exclude deleted/expired/foreign entries, bound counts/text, escape untrusted content.
- [ ] Add opt-in native due-review preparation through existing per-user job/tick, keyed agent+scope+local-date+kind for duplicate prevention. Pending bundle creation is not delivery; use existing governed fresh-authorized route only if its adapter reports success. Otherwise retain pending status for manual viewing. Never invent durable cold-start route, isolated OpenClaw session or receipt.
- [ ] Preserve operator schedule overrides. Test missed tick/restart, DST repeated hour, timezone change, no authorized route, failed send versus completed send, current-scope gating and disabled settings. Record actual scheduler precision and host-limited isolated-chat behavior explicitly, not blanket full parity. Commit.

## Audit boundary

Map declared OpenClaw tools/CLI/config to these reachable native counterparts.
Deletion and KNOWLEDGE updates retain explicit human confirmation. Exact OpenClaw
Gateway RPC, session-action and isolated-review delivery remain distinct host
contracts; missing ordinary native UI is not a host limitation.
