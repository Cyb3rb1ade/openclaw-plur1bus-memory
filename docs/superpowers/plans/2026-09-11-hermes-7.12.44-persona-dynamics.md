# Hermes 7.12.44 Persona and Dynamics Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Preserve persona seeds during evidence-based evolution and stop repeated memory-feedback reinforcement.

**Architecture:** Extend the managed native persona file with a protected seed boundary and keep evolution decisions separate from projection. Persist feedback consumption in the same per-card metadata update as strength, under the existing cross-process writer lock. Generic reply correlation remains an explicit host capability gap.

**Tech Stack:** Python, scoped JSON/JSONL, LanceDB, pytest/unittest.

**Spec:** `docs/superpowers/specs/2026-09-11-hermes-7.12.44-design.md`

## Global Constraints

- Pinned upstream `a3f48f28ac647e81c5260e8a1dbab7977bf9fb51`; Hermes baseline `c12ec2bba63d74ac8add8782ab6761472b4149c6`.
- No publication, production activation, live migrations or production package changes.
- Preserve private-scope opt-in, manual file sections, screening, symlink/revision guards, exact authorization and per-user job ownership.
- Required platforms macOS ARM64, Windows ARM64, Windows x64, Linux x64; retain Linux ARM64 coverage, no Intel macOS.
- Every behavior change requires RED/GREEN regression evidence and independent task review.
- Tests use `/Users/cyberblade/.hermes/hermes-agent/venv/bin/python -B` read-only with worktree PYTHONPATH and disposable fixtures.

## Task 1: Seed-preserving managed persona edits

**Files:** modify `plur1bus-hermes/src/plur1bus_hermes/persona_voice.py`; test `plur1bus-hermes/tests/test_persona_voice.py`.

**Interfaces:** Keep `evolve(workspace_dir, marker) -> bool` compatibility. Add `apply_evolution(workspace_dir, decision, *, max_bullets=24) -> dict` with result status `applied`, `duplicate`, `invalid`, `conflict` or `unavailable`. Decision is `{action: add|replace|none, marker: str, index?: int}`. Add seed boundary constant `SEED_END = "<!-- plur1bus:persona:seed-end -->"` and `read_managed_profile(workspace_dir) -> dict | None` returning seed/learned strings plus revision. Projection excludes the boundary itself.

- [ ] Add regression before implementation:

```python
write_seed(workspace, ["Warm greeting", "Concise sentences", "Gentle humor"])
for index in range(30):
    self.assertTrue(evolve(workspace, f"Distinct rhythm number {index}"))
profile = read_managed_profile(workspace)
self.assertEqual(profile["seed"], ["Warm greeting", "Concise sentences", "Gentle humor"])
self.assertEqual(len(profile["seed"]) + len(profile["learned"]), 24)
```

- [ ] Run persona suite RED. Write seed boundary after all generated seed bullets. For legacy files without boundary, protect first three bullets as upstream does; record this compatibility rule explicitly.
- [ ] Implement learned-only oldest-line rotation. Preserve all raw seed/manual lines even if projection screens them out. Never make the cap a reason to delete seed; if seed fills/exceeds cap, reject new learned addition with explicit reason. REPLACE uses one-based learned index only; invalid numeric index may fall back to ADD, but revision/I/O failure must never become an ADD retry.
- [ ] Keep existing unique temporary file/exact revision replacement guard. Malformed or duplicate boundaries fail closed; no rewriting manual-only files. No unsafe marker accepted through replacement or duplicate shortcut.
- [ ] Test seed sizes 3 and 8; legacy three-line fallback; valid/invalid REPLACE; duplicate/no-op; custom cap; oversize seed; manual surrounding text byte preservation; symlink and replacement races. Run existing projection/private-scope tests unchanged, commit exact files and evidence.

## Task 2: Evidence consumption and native daily persona evolution

**Files:** create `plur1bus-hermes/src/plur1bus_hermes/persona_evolution.py`; modify persona domain methods in `domain.py`, `jobs.py`, persona controls branch in `plur1bus-controls/src/plur1bus_controls/plugin.py`; tests `test_persona_voice.py`, new `test_persona_evolution.py`, `test_jobs.py` and relevant controls tests.

**Interfaces:** Keep `domain.evolve_persona_voice(outcomes=None, *, existing scope kwargs)`. Add `domain.run_persona_evolution(**scope_kwargs)` loading only scoped `.adaptive-learning/reply-outcomes.jsonl`. The loader accepts explicit matching agent/scope, stable event ID, valid timestamp not in the future, known outcome label and nonempty assistantText. Do not reinterpret memory-usefulness feedback as style evidence, join transcripts by text, or add a public arbitrary outcome-ingestion tool. Current public Hermes host has no generic authenticated delivery/reply contract, so an empty ledger returns `host-outcomes-unavailable` without LLM call. Report this remaining limitation honestly.

- [ ] Add stateful regression with ten distinct scoped outcomes in last seven days, six positive and four negative, controlled backend returning NONE:

```python
first = domain.evolve_persona_voice(outcomes)
self.assertEqual(first["reason"], "llm-none")
second = domain.evolve_persona_voice(outcomes)
self.assertEqual(second["reason"], "too-few-outcomes")
self.assertEqual(backend.calls, 1)
```

- [ ] Run RED; implement seven-day window, default ten NEW eligible outcomes, strict positive/(positive+negative) > 0.5, default two-day cooldown after actual change, heartbeat exclusion by session and prompt prefix. Reject bool/nonfinite timestamps; do not consume future, foreign or unrecognized events. Bound batch/evidence memory use; choose newest six positive and six negative snippets, each userPrompt/assistantText/replyText <=400 characters. Explicitly mark snippets as untrusted data in the internal JSON prompt.
- [ ] Use backend.complete_json with purpose `persona-evolution`, JSON action add/replace/none, screened marker and learned index; no request_seed reuse. Include protected seeds and numbered learned lines, bounded profile 4000 characters. Configuration `personaVoice.maxBullets`, `minDaysBetween`, `minOutcomes` gets defensive validation; missing values use 24/2/10. No hidden backend/model ownership change.
- [ ] Persist state atomically inside the scoped workspace, with bounded consumed event identities for the seven-day window rather than timestamp-only skipping (equal timestamps must remain distinguishable). NONE and duplicate consume evidence but do not advance lastEvolvedAt. Invalid LLM response or write conflict does not consume it. Corrupt existing state must return explicit failure, not silently reset cooldown.
- [ ] Use a recoverable prepared decision carrying before/after persona digest and consumed identities before file mutation: on restart finalize only an exact matching after digest; retry only exact before digest; unexpected manual edit returns conflict. Do not claim atomic two-file commit. Never fall back from a failed state write into untracked mutation.
- [ ] Route both command and daily private-scope job through the same domain method. Keep prior controls authorization and job gate/operator overrides. Disabled/shared/group cases never call LLM or touch private state. This job may validly report host-outcomes-unavailable until a separately verified host adapter exists.
- [ ] Test equal timestamps/different IDs, duplicates/restarts, 50% rejection, heartbeat/future/foreign exclusion, seed protection, NONE/duplicate no cooldown, actual ADD/REPLACE cooldown, state write failure/recovery/manual drift, empty host ledger, and reachable command/job wiring. Commit with explicit remaining host gap.

## Task 3: Idempotent scoped feedback dynamics

**Files:** modify `plur1bus-hermes/src/plur1bus_hermes/domain.py` run_dynamics; create `plur1bus-hermes/tests/test_feedback_dynamics.py`; extend existing scope/metadata tests where needed.

**Interfaces:** Preserve `run_dynamics(**scope_kwargs) -> {changed: int, ...}`. Store applied feedback event IDs inside each card's `metadataJson` in the SAME update as its new strength. Legacy feedback without IDs gets stable identity from canonical scoped row content plus duplicate occurrence ordinal, never random per run. Preserve all feedback log evidence.

- [ ] Add real-LanceDB fixed-clock regression:

```python
domain.run_dynamics()
once = metadata_for(card_id)["memoryStrength"]
domain.run_dynamics()
self.assertEqual(metadata_for(card_id)["memoryStrength"], once)
```

- [ ] Observe RED from repeated positive/negative reinforcement. Decorate mutation with serialized_memory_write. Read exact scoped rows and use per-row update, never recreate the entire metadata table in overwrite mode. Validate ID and scope in every write; legacy metadata without safe top-level scope stays unchanged with explicit skip count.
- [ ] Compute each unapplied feedback event once, preserve half-life elapsed-time decay and core/neverForget exemption. Distinguish numeric zero strength from absent strength (`0` must not become `1.0`). Keep applied identities while their source log remains present; do not trim a set in a way that re-enables old events. If storage size requires a cap, stop new event application with explicit bounded result rather than replaying old entries.
- [ ] Persist strength, lastDynamicsAt and consumed identities in one verified row update; unrelated metadata survives. On interrupted run only already committed rows consume events. Unknown or foreign event IDs do not grant ownership. Return scoped counts, no raw text/foreign IDs in logs.
- [ ] Test restart after partial row updates, repeated negative feedback, zero strength, old idless events, two legitimate identical feedback submissions, foreign scope same-ID negative case, concurrent capture preservation, core exemption and ordinary time decay. Run native metadata/scope/feedback/job suites; commit audit evidence.

## Review boundaries

This plan closes managed editing, evolution policy and feedback replay contracts. Generic authenticated reply-outcome collection remains a verified host API gap, not an implemented feature. Off-turn emotion refinement, reminders and Neo indexing require their own inspected contracts; none is claimed covered by these tests. Continue with full parity inventory after review.
