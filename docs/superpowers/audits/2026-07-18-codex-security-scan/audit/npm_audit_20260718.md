# npm audit — runtime dependency check

Command: npm audit --omit=dev --audit-level=high
Date: 2026-07-18

Result: exit 1, 3 high-severity dependency findings.

- adm-zip 0.5.17 is locked through optional @huggingface/transformers 4.2.0 → onnxruntime-node 1.24.3 → adm-zip ^0.5.16.
- npm audit identifies GHSA-xcpc-8h2w-3j85 / CVE-2026-39244 and reports a forced upgrade path through a breaking @huggingface/transformers version.
- The package is optional, but the lockfile resolves the vulnerable chain. No dependency update was performed in this audit.

