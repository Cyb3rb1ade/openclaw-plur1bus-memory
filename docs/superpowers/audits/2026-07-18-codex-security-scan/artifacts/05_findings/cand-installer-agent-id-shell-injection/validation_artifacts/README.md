# Reproduction

Run `PLUR1BUS_SNAPSHOT=<immutable snapshot> node installer-repro.mjs agent-shell repro-work`. The harness creates and deletes only `repro-work`, uses a stub `openclaw`, and writes a benign marker there.
