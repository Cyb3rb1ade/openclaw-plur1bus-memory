# Reproduction

Run `PLUR1BUS_SNAPSHOT=<immutable snapshot> node installer-repro.mjs workspace-shell repro-work`. It invokes only a disposable dry-run installer target and writes a benign marker there.
