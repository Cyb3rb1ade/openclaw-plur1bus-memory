# Hermes 7.12.47 incremental target

User instruction: incorporate 7.12.47, check functionality, build packages.
This extends the approved 7.12.44 native-port design; it does not erase unfinished
contracts or authorize a public release or production profile/data changes.

Upstream pin: `8a148c991123be31bc4f99376c8198447d9bb717` (`v7.12.47`).
Prior audited pin: `a3f48f28ac647e81c5260e8a1dbab7977bf9fb51`.
Hermes baseline remains `c12ec2bba63d74ac8add8782ab6761472b4149c6`.
Existing isolated branch/worktree stays in use to preserve reviewed work.
New candidate identifiers: JS/dashboard `7.12.47-hermes.0`, Python `7.12.47`.

Required additions are scope-key-correct partition guards, sparse ACL reads,
bounded resumable decay with durable partition-owned progress, batch efficiency
without duplicate decay on partial failure, and bounded optimize conflict retries
with truthful attempt/deadline reporting. Native metadata layout differs from JS;
literal two-statement SQL copying is not a correctness requirement. Feedback
idempotence, zero-strength preservation and atomic per-row strength/timestamp
updates remain mandatory. No broad table overwrite under concurrent capture.

Preserve all prior Hermes profile, controls, UI, provider, migration, recovery,
signing capability and platform requirements. Packages must identify actual
source and partial/unverified gates. macOS ARM64, Windows x64, Windows ARM64,
Linux x64 are required; retain Linux ARM64 coverage, no Intel macOS.
Building a candidate does not certify full parity or authorize publication.
Security audit failures remain visible blockers to release readiness.

Historical audit pins/reports remain immutable; add new delta evidence separately.
