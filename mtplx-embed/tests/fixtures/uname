#!/usr/bin/env bash
set -euo pipefail
case "${1:-}" in
  -s) printf '%s\n' "${MTPLX_TEST_OS:-Linux}" ;;
  -m) printf '%s\n' "${MTPLX_TEST_ARCH:-x86_64}" ;;
  *) printf '%s\n' "${MTPLX_TEST_OS:-Linux}" ;;
esac
